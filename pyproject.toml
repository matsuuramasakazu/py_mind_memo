[tool.ruff]
# チェックするルールの選択 (E: pycodestyle, F: Pyflakes, I: isort等)
select = ["E", "F", "I", "B"]
ignore = ["E701", "I001", "E501", "F401"]

# 1行の最大文字数
line-length = 120

[tool.ruff.format]
# ダブルクォートを優先する（Black互換）
quote-style = "double"
